# Sample environment variables
# Copy this file to .env and modify as needed

# Application
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# Database (not used in this simple example)
DB_HOST=localhost
DB_USER=user
DB_PASSWORD=password